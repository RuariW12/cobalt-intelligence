# The image copies only requirements.lock, app/, store/, etl/ and web/.
# Everything else is build context the daemon would ship for no reason.
.git
.gitignore
.venv/
.vscode/
.env
__pycache__/
*.pyc
context/
docker/
config/
*.md
LICENSE
cobalt-start.sh
cobalt-stop.sh
